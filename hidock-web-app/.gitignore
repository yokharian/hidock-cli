# General rules for logs, editor files, and custom folders like 'archived/'
# are now handled by the root .gitignore file for project-wide coverage.

# Node.js dependencies
# Also in root .gitignore, but good practice for portability.
node_modules

# Next.js build outputs and cache
dist
dist-ssr
*.local
.next/

# Next.js specific environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Optional PWA files
public/sw.js
public/workbox-*.js
